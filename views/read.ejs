<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="p-10 bg-zinc-900 min-h-screen">
        <div class="mb-12">
            <a href="/" class="text-blue-500">Go back to home</a>
        </div>
        <% if(users.length>0){ %>
            <h1 class="text-3xl tracking-tighter mb-3 text-white text-center">All Users</h1>
        <% } %>
        <div class="users flex gap-3 flex-wrap justify-evenly mt-12">
            <% if(users.length>0){ %>
                <%users.forEach(function(user){%>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-60 rounded-lg bg-red-500">
                            <img class="w-full h-full object-cover" src="<%= user.imageUrl%>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter text-white"><%= user.name%></h3>
                        <h5 class="text-zinc-500"><%= user.email%></h5>
                        <div class="flex gap-3 justify-between">
                            <a href="/edit/<%= user._id%>" class="text-yellow-400">Edit user</a>
                            <a href="/delete/<%=user._id%>" class="text-red-400">delete user</a>
                        </div>
                    </div>
                <%})%>
            <%} else {%>
                <h1 class="text-white text-3xl">No users Created yet</h1>
            <%}%>
        </div>
    </div>
</body>
</html>